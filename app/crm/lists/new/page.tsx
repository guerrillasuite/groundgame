import CreateListWizard from "@/app/components/lists/CreateListWizard";

export const metadata = { title: "Create List | GroundGame" };

export default function NewListPage() {
  return <CreateListWizard />;
}
